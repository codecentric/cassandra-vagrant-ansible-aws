# Insert your information for your AWS account in here

# Basic key/secret access to AWS
key: KEY
secret: SECRET

# Cassandra topology and coupled to this, AWS access/machine information (which is region specific)

datacenters:
  -
  	# The region to setup the nodes eg. us-west-1
    region: AWS-Region 
	number_of_nodes: 5
	# The image to use for the box which must be available in the region eg. ami-fa5843bf
	ami: AMIIDENTIFIER
	keyname: KEYNAME
	keypath: PATH TO PRIVATE KEY PEM FILE